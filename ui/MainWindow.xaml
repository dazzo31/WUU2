<Window
    xmlns='http://schemas.microsoft.com/winfx/2006/xaml/presentation'
    xmlns:x='http://schemas.microsoft.com/winfx/2006/xaml'
    x:Name='Window' Title='PowerShell Windows Update Utility' WindowStartupLocation = 'CenterScreen' 
    Width = '1050' Height = '600' ShowInTaskbar = 'True'
    Background="#1E1E2E" WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <!-- Modern Dark Theme Palette -->
        <SolidColorBrush x:Key="PrimaryBackground" Color="#1E1E2E"/>  <!-- Deep Charcoal -->
        <SolidColorBrush x:Key="SecondaryBackground" Color="#252538"/>  <!-- Slate -->
        <SolidColorBrush x:Key="TertiaryBackground" Color="#2D2D38"/>  <!-- Darker Slate -->
        <SolidColorBrush x:Key="AccentColor" Color="#00ADB5"/>  <!-- Electric Teal -->
        <SolidColorBrush x:Key="AccentDark" Color="#007F8A"/>  <!-- Darker Teal (hover) -->
        <SolidColorBrush x:Key="TextPrimary" Color="#E3E3E6"/>  <!-- Soft White -->
        <SolidColorBrush x:Key="TextSecondary" Color="#A0A0A5"/>  <!-- Muted Gray -->
        <SolidColorBrush x:Key="Borders" Color="#3F3F46"/>  <!-- Muted Grey -->
        <SolidColorBrush x:Key="Success" Color="#2ECC71"/>  <!-- Emerald Green -->
        <SolidColorBrush x:Key="Warning" Color="#F39C12"/>  <!-- Amber -->
        <SolidColorBrush x:Key="Error" Color="#E74C3C"/>  <!-- Alizarin Crimson -->
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Borders}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Modern Label Style -->
        <Style x:Key="ModernLabel" TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>
        
        <!-- Header Label Style -->
        <Style x:Key="HeaderLabel" TargetType="Label" BasedOn="{StaticResource ModernLabel}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Borders}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Header Template for GridView with modern styling -->
        <!-- NOTE: the resize grip (PART_HeaderGrip) lives in GridViewColumnHeaderStyle's
             ControlTemplate, NOT here - GridViewColumnHeader only hooks a Thumb named
             PART_HeaderGrip inside its own template. -->
        <DataTemplate x:Key="HeaderTemplate">
            <Grid Background="{StaticResource SecondaryBackground}">
                <TextBlock FontSize="12" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" Margin="8,6" Text="{Binding}"/>
            </Grid>
        </DataTemplate>
        
        <!-- GridView Column Header Style for dark theme -->
        <Style x:Key="GridViewColumnHeaderStyle" TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Borders}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter Margin="{TemplateBinding Padding}" 
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                            </Border>
                            <!-- The Thumb MUST be named PART_HeaderGripper (with 'er') -
                                 WPF's GridViewColumnHeader.HookupGripperEvents looks up
                                 exactly this name to wire its native drag-resize handlers.
                                 (Commonly-documented 'PART_HeaderGrip' is WRONG.) -->
                            <Thumb x:Name="PART_HeaderGripper" 
                                   HorizontalAlignment="Right" 
                                   VerticalAlignment="Stretch" 
                                   Width="4" 
                                   Cursor="SizeWE" 
                                   Background="Transparent"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="Floating">
                                <Setter TargetName="PART_HeaderGripper" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ListView Item Style for better readability -->
        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource TertiaryBackground}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Modern Context Menu Style -->
        <Style x:Key="ModernContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Borders}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Context Menu Item Style -->
        <Style x:Key="ContextMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Alternating Row Style with modern colors -->
        <Style x:Key="AlternatingRowStyle" TargetType="{x:Type Control}">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UpdatesStatus}" Value="All updates installed">
                    <Setter Property="Background" Value="{StaticResource Success}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding UpdatesStatus}" Value="Updates required">
                    <Setter Property="Background" Value="{StaticResource Error}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding UpdatesStatus}" Value="Unknown">
                    <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding UpdatesStatus}" Value="Reboot required">
                    <Setter Property="Background" Value="{StaticResource Warning}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid x:Name = 'Grid' ShowGridLines = 'false' Background="{StaticResource PrimaryBackground}" Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height = 'Auto'/>
            <RowDefinition Height = 'Auto'/>
            <RowDefinition Height = '*'/>
            <RowDefinition Height = 'Auto'/>
            <RowDefinition Height = 'Auto'/>
            <RowDefinition Height = 'Auto'/>
        </Grid.RowDefinitions>
        <Menu Width = 'Auto' HorizontalAlignment = 'Stretch' Grid.Row = '0' Background="{StaticResource SecondaryBackground}" FontSize="13" Margin="0,0,0,10" BorderThickness="0">
            <Menu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                    <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Borders}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontFamily" Value="Segoe UI"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="Margin" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="Separator">
                    <Setter Property="Background" Value="{StaticResource Borders}"/>
                    <Setter Property="Height" Value="1"/>
                    <Setter Property="Margin" Value="5,2"/>
                </Style>
            </Menu.Resources>
            <MenuItem x:Name = 'FileMenu' Header = '_File'>
                <MenuItem x:Name = 'AddComputerMenu' Header = 'Add Computers' ToolTip = 'Add computers by name'/>
				<MenuItem x:Name = 'AddADMenu' Header = 'Add Computers From AD' ToolTip = 'Add computers by selecting an OU from Active Directory'/>
                <MenuItem x:Name = 'BrowseFileMenu' Header = 'Add Computers From File' ToolTip = 'Import computers from CSV or TXT file with advanced options' InputGestureText ='Ctrl+O'/>
                <Separator />
                <MenuItem x:Name = 'SaveConfigMenu' Header = 'Save Computer List (Encrypted)' ToolTip = 'Saves the current computer list to an encrypted configuration file'/>
                <MenuItem x:Name = 'LoadConfigMenu' Header = 'Load Computer List (Encrypted)' ToolTip = 'Loads a computer list from an encrypted configuration file'/>
				<MenuItem x:Name = 'ExportListMenu' Header = 'Export Computer List' ToolTip = 'Creates a list of all computers and saves to a text file.' InputGestureText ='Ctrl+S'/>
                <Separator />
                <MenuItem x:Name = 'ExitMenu' Header = 'Exit' ToolTip = 'Exits the utility.'/>
            </MenuItem>
            <MenuItem x:Name = 'EditMenu' Header = '_Edit'>
                <MenuItem x:Name = 'SelectAllMenu' Header = 'Select _All' ToolTip = 'Selects all rows.' InputGestureText ='Ctrl+A'/>
            </MenuItem>
            <MenuItem x:Name = 'ActionMenu' Header = '_Action'>
				<MenuItem x:Name = 'ClearComputerListMenu' Header = 'Clear Computer List' ToolTip = 'Clears the computer list.'/>
				<MenuItem x:Name = 'OfflineHostsMenu' Header = 'Remove Offline Computers' ToolTip = 'Removes all offline hosts from Computer List'/>
				<Separator />
				<MenuItem x:Name = 'TestADConnectionMenu' Header = 'Test AD Connection' ToolTip = 'Test Active Directory connectivity and diagnose issues'/>
				<Separator />
                <MenuItem x:Name = 'ViewErrorMenu' Header = 'View ErrorLog' ToolTip = 'Opens error log.' IsEnabled="false"/>
            </MenuItem>
        </Menu>
        <Grid Grid.Row = '2' Grid.Column = '0' ShowGridLines = 'false' Background="{StaticResource PrimaryBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
                <!--<ColumnDefinition Width="10"/>-->
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height = 'Auto'/>
                <RowDefinition Height = 'Auto'/>
                <RowDefinition Height = '*'/>
                <RowDefinition Height = '*'/>
                <RowDefinition Height = 'Auto'/>
                <!--<RowDefinition Height = 'Auto'/>-->
                <RowDefinition Height = 'Auto'/>
            </Grid.RowDefinitions>
			<CheckBox x:Name='AutoDownloadCheckBox' Grid.Column='0' Grid.Row='0' ToolTip='Automatically download available updates after checking for updates' Style="{StaticResource ModernCheckBox}" Margin="5">Auto Download</CheckBox>
			<CheckBox x:Name='AutoInstallCheckBox' Grid.Column='1' Grid.Row='0' ToolTip='Automatically install updates once they are downloaded' Style="{StaticResource ModernCheckBox}" Margin="5">Auto Install</CheckBox>
			<CheckBox x:Name='AutoRebootCheckBox' Grid.Column='2' Grid.Row='0' ToolTip='Automatically reboot computers after installing updates if a reboot is required' Style="{StaticResource ModernCheckBox}" Margin="5">Auto Reboot</CheckBox>
            <GroupBox Header = "Computer List" Grid.Column = '0' Grid.Row = '2' Grid.ColumnSpan = '11' Grid.RowSpan = '3' Background="{StaticResource SecondaryBackground}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource Borders}" BorderThickness="1" Padding="10" FontWeight="Bold" FontFamily="Segoe UI" FontSize="14" Margin="0,10,0,0">
                <ListView x:Name = 'Listview' AllowDrop = 'True' AlternationCount="2" ItemContainerStyle="{StaticResource ListViewItemStyle}" Background="{StaticResource SecondaryBackground}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource Borders}" BorderThickness="0" FontSize="13" FontFamily="Segoe UI">
                    <ListView.View>
                        <GridView x:Name = 'GridView' AllowsColumnReorder = 'True' ColumnHeaderTemplate="{StaticResource HeaderTemplate}" ColumnHeaderContainerStyle="{StaticResource GridViewColumnHeaderStyle}">
                            <GridViewColumn x:Name = 'ComputerColumn' Width = '100' DisplayMemberBinding = '{Binding Path = Computer}' Header='Computer'/>
                            <GridViewColumn x:Name = 'PhaseColumn' Width = '70' DisplayMemberBinding = '{Binding Path = Phase}' Header='Phase'/>
                            <GridViewColumn x:Name = 'AvailableColumn' Width = '80' DisplayMemberBinding = '{Binding Path = Available}' Header='Available'/>
                            <GridViewColumn x:Name = 'DownloadedColumn' Width = '80' DisplayMemberBinding = '{Binding Path = Downloaded}' Header='Downloaded' />
							<GridViewColumn x:Name = 'InstallErrorsColumn' Width = '80' DisplayMemberBinding = '{Binding Path = InstallErrors}' Header='Install Errors' />
                            <GridViewColumn x:Name = 'StatusColumn' Width = '295' DisplayMemberBinding = '{Binding Path = Status}' Header='Status'/>
							<GridViewColumn x:Name = 'RebootColumn' Width = '80' DisplayMemberBinding = '{Binding Path = RebootRequired}' Header='Reboot Required'/>
							<GridViewColumn x:Name = 'UpdatesStatusColumn' Width = '120' DisplayMemberBinding = '{Binding Path = UpdatesStatus}' Header='Updates Status'/>
                        </GridView>
                    </ListView.View>
                    <ListView.ContextMenu>
                        <ContextMenu x:Name = 'ListViewContextMenu' Style="{StaticResource ModernContextMenu}">
                            <ContextMenu.Resources>
                                <Style TargetType="MenuItem" BasedOn="{StaticResource ContextMenuItemStyle}"/>
                            </ContextMenu.Resources>
                            <MenuItem x:Name = 'AddComputerContext' Header = 'Add Computers'/>
                            <MenuItem x:Name = 'AddADContext' Header = 'Add Computers From AD'/>
							<MenuItem x:Name = 'AddFileContext' Header = 'Add Computers From File' InputGestureText ='Ctrl+O'/>
                            <MenuItem x:Name = 'RemoveComputerContext' Header = 'Remove Computer' InputGestureText ='Del'/>
                            <Separator />
                            <MenuItem x:Name = 'CopyCellContext' Header = 'Copy Cell Content' InputGestureText ='Ctrl+C' ToolTip = 'Copy the content of the selected cell to clipboard'/>
                            <Separator />
                            <MenuItem x:Name = 'AssignPhaseMenu' Header = 'Assign Phase'>
                                <MenuItem x:Name = 'Phase1Menu' Header = 'Phase 1'/>
                                <MenuItem x:Name = 'Phase2Menu' Header = 'Phase 2'/>
                                <MenuItem x:Name = 'Phase3Menu' Header = 'Phase 3'/>
                                <MenuItem x:Name = 'Phase4Menu' Header = 'Phase 4'/>
                                <MenuItem x:Name = 'Phase5Menu' Header = 'Phase 5'/>
                            </MenuItem>
                            <Separator />
							<MenuItem x:Name = 'RemoteDesktopContext' Header = 'Remote Desktop'/>
							<MenuItem x:Name = 'CheckUpdatesContext' Header = 'Check For Updates'/>
							<MenuItem x:Name = 'DownloadUpdatesContext' Header = 'Download Updates'/>
							<MenuItem x:Name = 'InstallUpdatesContext' Header = 'Install Updates'/>
							<MenuItem x:Name = 'RestartContext' Header = 'Restart Computer'/>
							<Separator />
							<MenuItem x:Name = 'ShowUpdatesContext' Header = 'Show Available Updates'/>
							<MenuItem x:Name = 'ShowInstalledContext' Header = 'Show Installed Updates'/>
							<MenuItem x:Name = 'WSUSAuditContext' Header = 'Audit WSUS Updates' ToolTip = 'Compare WSUS-approved updates with Windows Update counts'/>
							<MenuItem x:Name = 'UpdateHistoryMenu' Header = 'Show Update History'/>
							<MenuItem x:Name = 'ViewUpdateLogContext' Header = 'View Windows Update Log'/>
                            <MenuItem x:Name = 'WindowsUpdateServiceMenu' Header = 'Windows Update Service'>
                                <MenuItem x:Name = 'WUStopServiceMenu' Header = 'Stop Service'/>
                                <MenuItem x:Name = 'WUStartServiceMenu' Header = 'Start Service'/>
                                <MenuItem x:Name = 'WURestartServiceMenu' Header = 'Restart Service'/>
                            </MenuItem>
                            <Separator />
                            <MenuItem x:Name = 'SetDomainCredentialsContext' Header = 'Set Domain Credentials' ToolTip = 'Configure domain credentials for remote WMI/RPC access'/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </GroupBox>
        </Grid>
        <TextBox x:Name = 'StatusTextBox' Grid.Row = '4' ToolTip = 'Displays current status of operation' Background="{StaticResource SecondaryBackground}" Foreground="{StaticResource TextPrimary}" BorderBrush="{StaticResource Borders}" BorderThickness="1" Padding="10,8" FontSize="13" FontFamily="Segoe UI" Margin="0,10,0,0"> Waiting for Action... </TextBox>
    </Grid>
</Window>